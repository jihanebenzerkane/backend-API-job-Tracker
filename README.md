# backend-API-job-Tracker
This project is a RESTful backend API built with FastAPI that allows users to manage job and internship applications. It provides endpoints to create and retrieve users, as well as create and track job applications linked to specific users.
